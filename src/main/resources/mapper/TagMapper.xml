<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.b3log.zephyr.dao.TagDAO">
    <resultMap id="welcomTagModel" type="org.b3log.zephyr.model.welcome.WelcomeTagModel">
        <result column="tag_name" property="tagName"/>
        <result column="tag_count" property="tagCount"/>
        <result column="creator" property="tagCreator"/>
    </resultMap>

    <parameterMap id="Taglib" type="org.b3log.zephyr.element.entity.TagLib"/>

    <sql id="Columns">
        message_id, tag_id, content, creator, update_time, create_time
    </sql>

    <!--<select id="findByName" resultMap="BaseResultMap" parameterType="java.lang.String">-->
    <!--select-->
    <!--<include refid="Base_Column_List" />-->        <!--from city-->
    <!--where city_name = #{cityName}-->
    <!--</select>-->

    <!--<select id="findAllMessages">
        SELECT
        <include refid="Columns"/>
        FROM messagelib ORDER BY create_time DESC
    </select>-->

    <select id="findTagsByUserId" resultMap="welcomTagModel">
        select t.tag_name,m.creator,count(1) as tag_count from messagelib m left join taglib t on m.tag_id=t.tag_id
        where m.creator=#{uid}
        group by m.tag_id,m.creator
    </select>

    <select id="findByTagName" resultMap="org.b3log.zephyr.element.entity.TagLib">
        SELECT * FROM taglib WHERE tag_name=#{tagName}
    </select>

    <insert id="saveTag" parameterMap="org.b3log.zephyr.element.entity.TagLib">
        INSERT INTO
            taglib VALUES
            (
                #{tag_id},
                #{tag_name},
                #{tag_logo},
                #{description},
                #{creator},
                #{create_time}
            )
    </insert>
</mapper>
